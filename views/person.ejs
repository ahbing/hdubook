<%- include header %>
  <%= title%>
 <img src="/upload/images/<%= user.header%>" alt="用户头像"/>
 <a href="/sold/<%= userName%>">我賣掉的書</a>
 <a href="/edit/<%= userName%>">個人信息修改</a>
  <% books.forEach(function(book,index){ %>
<% if(book.sold == 0){ %>
  <div>
  書名:<a href="/book/<%= book._id %>"><%= book.bookname %></a><br/>
  價格:<%= book.bookprice %><br/><br/>
  我曾經使用的時間:<br/><%= book.usetime %><br/><br/>
  我對浏览者的留言:<br/><%= book.usersay %><br/>
  <a href="edit/<%= book._id %>">編輯</a>
  <a href="delete/<%= book._id%>">不賣啦</a>
  <a href="sold/<%= book._id%>">賣掉啦</a>
  </div>
  <br />
<% } %>
  <% }); %>


    <div>
    <% if (!isFirstPage) { %>
      <span><a title="上一页" href="?p=<%= page-1 %>">上一页</a></span>
    <% } %>
    <% if (!isLastPage) { %>
      <span><a title="下一页" href="?p=<%= page+1 %>">下一页</a></span>
    <% } %>
  </div>
<%- include footer %>